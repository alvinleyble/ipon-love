<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- allowBackup off deliberately: Android's Auto Backup restores this app's private storage
         (DataStore prefs incl. the app-lock PIN, Room cache) on reinstall/device-transfer,
         before the app's own account-switch wipe ever runs — leaking one account's local state
         into whoever logs in next. Supabase is the source of truth, so nothing here needs to
         survive a reinstall. -->
    <application
        android:name=".IponApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.IponLove">

        <!--
            Disable WorkManager's auto-init ContentProvider so the custom Configuration
            (with HiltWorkerFactory) from IponApp is used instead of the default factory.
            Without this, @HiltWorker classes fail with "Could not create Worker".
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.IponLove">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Catches the Supabase email-confirmation redirect after the user taps the link -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.iponlove.app" android:host="login-callback" />
            </intent-filter>
        </activity>

        <!-- Shares exported CSV/PDF/ZIP files from cacheDir/exports via the share sheet
             (v1.7.0 Item 6). Authority tracks the per-flavor applicationId so context.packageName
             always resolves to the same authority. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".feature.widget.presentation.QuickAddActivity"
            android:exported="false"
            android:theme="@style/Theme.IponLove.Transparent"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

        <receiver
            android:name=".feature.widget.presentation.AddTransactionWidgetReceiver"
            android:label="@string/add_transaction_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/add_transaction_widget_info" />
        </receiver>

        <receiver
            android:name=".feature.widget.presentation.BalanceWidgetReceiver"
            android:label="@string/balance_widget_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/balance_widget_info" />
        </receiver>
    </application>

</manifest>
