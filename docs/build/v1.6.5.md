# V1.6.5 — Multi-image receipts

**Status:** Code `DONE a19e910` 2026-07-07, build green (`testStagingDebugUnitTest` + `assembleStagingDebug`). **Not yet on-device verified; staging migration not yet applied by Alvin.**

The single-photo receipt (`transactions.attachment_url`, one per row) is replaced by an up-to-3 multi-image child table `transaction_images`, mirroring the notes attachment pattern end-to-end (entity → DAO → DTO → mapper → uploader → owned + partner syncers → redacting view → SECURITY DEFINER Storage RLS → strip UI → combined-view thumbnail). Opus, high effort — cross-ADR (0009 FK order, 0005 partner views, 0011/0004 combined view, 0043 Storage RLS) with a schema migration.

Grilled before building (two real forks resolved with Alvin):
1. **Pre-save persistence model → editor-state list, reconcile on save.** The transaction editor defers *all* persistence to Save (today's receipt lives only in editor state + `SavedStateHandle`; no row exists until save), unlike notes whose parent row already exists. So the draft holds a `List<TransactionImage>`; on save `SaveTransactionImagesUseCase` diffs it against the DB (insert new picks, soft-delete removed) — never creating orphan child rows for an abandoned add.
2. **Migration → backfill then drop, clean.** Dev-phase (testdevs only), so each non-null `attachment_url` is backfilled into one `transaction_images` row (keeping the URL verbatim, no Storage re-keying) and the column is dropped from `transactions` + `partner_transactions`. The `attachmentUrl`/`attachmentLocalPath` Room columns are dropped too (v23 `@DeleteColumn`); no local data backfill — devices pull the server-backfilled rows on the new table's cursor-0.
3. Combined-view thumbnail → one thumbnail + count badge (not a stack), tap opens a `HorizontalPager`.

---

## What was built

**Schema** (`supabase/migrations/2026-07-07_transaction_images.sql` + `schema.sql`):
- New `transaction_images` table (`id, transaction_id FK, storage_url, position, created_at, updated_at, is_deleted, server_rev`) + `set_server_rev` trigger + rev/txn indexes + owner RLS (gated through the parent transaction's ownership).
- `partner_transaction_images` redacting view mirroring `partner_note_images` — `storage_url` nulled when `ti.is_deleted OR t.is_private OR t.is_deleted` so removals propagate as a purge signal. Granted to `authenticated`.
- Backfill `INSERT … SELECT` from `attachment_url`, then `DROP COLUMN attachment_url` (+ rebuilt `partner_transactions` without it).
- `partner_can_read_receipt(object_name)` SECURITY DEFINER fn re-pointed at `transaction_images` joined to `transactions` (same ADR-0043 technique; unchanged non-private/non-deleted gate). Reuses the existing private `receipts` bucket + `receipts_partner_read` policy — no policy change. New object path `receipts/{userId}/{transactionId}/{imageId}.jpg` (folder[1] = userId keeps the owner policy satisfied).

**Room (v22 → v23):** new `TransactionImageEntity`/`Dao`/`Dto`/`PartnerDto`/`Mapper`, `TransactionImageUploader` (PreSyncStep, keyed on image id), `TransactionImageTableSyncer` + `PartnerTransactionImageTableSyncer`, `TransactionImageRepository`(+Impl), use cases `SaveTransactionImagesUseCase` (reconcile + `TransactionImage.MAX=3` backstop), `GetTransactionImagesUseCase`, `ObserveTransactionImageUrlsUseCase`, `CompressReceiptUseCase` (repurposed from `AttachReceiptUseCase`, now keyed on image id). `AutoMigration(22→23, spec = DeleteReceiptColumnsMigration)` auto-creates the table + drops the two dead transaction columns. `SyncTable`: `TRANSACTION_IMAGES` after `TRANSACTIONS`, `PARTNER_TRANSACTION_IMAGES` after `PARTNER_TRANSACTIONS` (ADR-0009). Unpair purge + partner-cursor reset wired in `PurgePartnerReplicaUseCase`.

**Removed single-photo path:** `attachmentUrl`/`attachmentLocalPath` off `TransactionEntity`/`Transaction`/`TransactionDto`/`PartnerTransactionDto` + mappers + repo; `ReceiptUploader` and `AttachReceiptUseCase` deleted; `TransactionDao` receipt-upload queries removed.

**UI:** `AddTransactionScreen`'s `ReceiptRow` → `ReceiptStrip` (horizontally-scrollable `LazyRow` of up to 3 thumbnails, `n/3` counter that reddens at the cap and hides while empty, add button greyed at the cap), each thumbnail opening the new `core/ui/FullScreenImagePager` (swipe between all receipts). `AddTransactionViewModel` holds the draft image list, mirrors it into `SavedStateHandle` as three parallel string lists, and reconciles on save. Combined view: `CombinedEntry.attachmentUrl` → `attachmentUrls: List<String>` (fed by `ObserveTransactionImageUrlsUseCase` → `Map<txnId, urls>` into `CombinedLedgerCalculator`); `CombinedRow` shows the first receipt + a count badge, tap → pager.

**Tests (JVM, all green):** `TransactionImageMapperTest` (Entity↔Domain↔DTO round-trip, un-uploaded `toDto` throws, partner null-url redaction preserved), `TransactionImageUploaderTest` (path construction, delete-pending cleanup, missing-file skip), `SaveTransactionImagesUseCaseTest` (new-txn insert-all, edit insert/delete diff, 3-cap backstop), `CombinedLedgerCalculatorTest` updated to carry the image-url list, `PurgePartnerReplicaUseCaseTest` asserts the new purge + cursor reset.

---

## Before this ships
- **Alvin applies `2026-07-07_transaction_images.sql` to staging** (backfill + drop; the migration is destructive on `attachment_url`, so app + schema must land together — testdevs reinstall).
- On-device verify: add/remove up to 3 receipts on a new and an edited transaction, swipe the full-screen pager, confirm partner receipts render in the combined view under the partner-read RLS, and confirm the 3-cap greys the add button.
