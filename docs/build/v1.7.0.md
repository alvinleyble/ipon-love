# V1.7.0 — running ledger of code changes

**Charter (set 2026-07-23):** the active running batch — successor to [v1.6.9.md](v1.6.9.md) (released to closed testing 2026-07-23). Same rule as every version doc — the running catch-all for every code change we accumulate, big or small, until it is built and committed. Any tweak, fix, copy change, or feature we decide on gets booked here as a numbered item first, so nothing slips between conversations.

**Status:** 🚧 **OPEN.**

---

## Item 1 — Seed testdev7/testdev8 with a full realistic data profile (Play Store screenshot candidates)

- **Status:** ✅ **DONE 2026-07-23** (Sonnet, medium effort — data population following established schema/entity patterns, not new architecture; the effort was in volume/internal-consistency across ~2 months of data for a paired couple). **Grilled first** to pin down both personas (income, accounts, goals, bills, spend shape), then written directly into the staging Supabase DB via the read-write MCP (`mcp__supabase__execute_sql`), respecting ADR-0001 (LWW `updated_at`/`server_rev`, both handled automatically — `server_rev` is trigger-assigned, `updated_at` defaults `now()` on insert), ADR-0002 (no local `pending_sync` involved — these are server-native rows, the app just needs a pull-sync to bring them into Room), ADR-0007 (balance derived — only `opening_balance` + ledger rows were written, current balance was never touched directly), ADR-0009 (inserted in FK order: accounts → categories → recurring_rules → transactions → savings_goals/goal_contributions), and ADR-0049 (`exclude_from_analysis` on the reimbursable pair). No schema/Room change — reused existing tables only.
- **Request:** Alvin — candidate couple for the eventual Play Store screenshot re-render ([deferred Item 41](deferred.md#item-41--play-store-screenshot-refresh-playful-pop-reskin), still held for the final shipping UI) is **testdev7 + testdev8** (confirmed actively paired 2026-07-04, `couple_id 82dba413-bb5b-4488-93c5-5af5431ffba2`). Wants both accounts fully populated — accounts (incl. savings), recurring bills, recurring salaries, reimbursables, and transactions spanning **June 2026–present** — so the screenshots show a lived-in app, not empty/sparse test data. Explicitly asked to profile the two personas in a grilling session before populating, and confirmed mid-build that population should happen now, not just be documented as a plan.
- **Relationship to Item 41:** data-prep only, not the screenshot capture/render pipeline itself (still correctly deferred until the reskin settles). Populating testdev7/8 now is independent prep that isn't blocked by that gate.
- **Grilled persona (locked 2026-07-23):**
  - **Juan (testdev7, accent `#1565C0`)** — salaried, ₱45,000/mo, paid **semi-monthly on the 15th & 30th** (₱22,500/cut) to his Bank account. Fronts occasional work expenses (Reimbursable/Reimbursement category pair, ADR-0049, excluded from Analysis).
  - **Maria (testdev8, accent `#C62828`)** — salaried, ₱32,000/mo, paid **semi-monthly on the 10th & 25th** (₱16,000/cut) to her Bank account.
  - **Accounts** (starter Cash/GCash/Bank Account kept per person, plus): a personal **Credit Card** each, and a couple-owned **Joint Bank** account (ADR-0018, `couple_id` set, created by Juan) carrying the shared rent/utilities.
  - **Opening balances** (as of 2026-06-01, before the seeded ledger): Juan — Cash ₱2,500 · GCash ₱4,000 · Bank ₱35,000 · Credit Card ₱0. Maria — Cash ₱1,800 · GCash ₱3,200 · Bank ₱22,000 · Credit Card ₱0. Joint Bank ₱15,000.
  - **Savings goals** (`savings_goals`/`goal_contributions`, monthly-lump cadence): shared **"Baguio Trip"** (target ₱80,000, due Dec 2026) — ₱17,000 from Juan in June + ₱17,000 from Maria in July → ₱34,000 (42.5%); Juan's personal **"New Laptop"** (₱65,000, due Oct 2026) — ₱9,000 × 2 → ₱18,000 (27.7%); Maria's personal **"Emergency Fund"** (₱50,000, no due date) — ₱16,000 × 2 → ₱32,000 (64%).
  - **Recurring bills** (`recurring_rules`, `auto_post=false` — confirm-on-arrival per ADR-0048, all owned by the payer): Joint account — Rent ₱18,000 (1st), Electricity ₱3,500 (5th), Water ₱800 (5th), Internet ₱2,000 (10th), all owned by Juan. Juan's Bank — Netflix ₱549 (15th), Spotify ₱149 (20th). Maria's Bank — Phone plan ₱1,299 (25th), Gym membership ₱1,500 (1st). Every rule's `next_date` was set to its true next occurrence **after** 2026-07-23 (Aug 1/5/10/15/20, or Jul 25/30 for the two cycles that hadn't recurred yet) — none left overdue/stuck-pending; the app's own confirm-on-arrival flow takes over cleanly from here.
  - **Reimbursable pattern (Juan only):** 3 expense/income pairs across June–July (Grab+parking ₱1,200, WFH dock ₱3,500, client-visit lunch ₱2,800), each followed 1–3 weeks later by a matching Reimbursement income — both categories flagged `exclude_from_analysis`.
  - **Everyday spend (June 1 – Jul 23, ~7.5 weeks):** Food & Dining (Mondays, Cash/GCash alternating), Transportation (Fridays), Groceries (Saturdays), Shopping (~every 10 days, on the Credit Card — the debt driver). Credit card partially paid down 2×/month via a Bank→Card transfer (not fully, so a real balance carries).
- **Bug caught + fixed during the build (not in the original grilled plan):** the first balance-verification pass found GCash and the Joint account going **unrealistically negative** (Joint: −₱33,600; Juan's GCash: −₱9,230) because the grilled plan modeled outflows (bills, groceries, transport) but never modeled money flowing *in* to those accounts. Fixed by adding: a monthly Joint-account contribution from each partner's Bank (Juan ₱15,000 + Maria ₱12,000, 2×) sized to cover the ₱24,300/mo shared-bill load with a buffer, and 4 Bank→GCash top-up transfers (₱3,500 each) for Juan to cover his weekly GCash-heavy grocery/transport spend. Re-verified clean afterward (see Build outcome).
- **Build outcome:** 93 transactions total (Juan: 56 — 6 income / 42 expense / 8 transfer; Maria: 37 — 3 income / 30 expense / 4 transfer), 12 recurring rules, 3 savings goals with 6 contribution rows, 4 new accounts (2 Credit Card + 1 Joint Bank), 4 new categories (2× Subscriptions, Reimbursable, Reimbursement), 2 users rows updated with display names. **Final verified balances** — Juan: Cash ₱815 · GCash ₱4,770 · Bank ₱54,104 · Credit Card −₱5,275. Maria: Cash ₱120 · GCash ₱815 · Bank ₱25,001 · Credit Card −₱1,400. Joint Bank ₱20,400. All positive except the two credit cards, which are meant to carry a debt.
- **Not yet done (next step, separate from this item):** neither account has pulled this data into its local Room DB yet — the seed lives only in staging Supabase. Whoever drives the screenshot session needs to log into `testdev7@iponlove.com`/`testdev8@iponlove.com` (staging) and let a pull-sync run before capturing anything.
- **Verify:** SQL-verified account balances (opening_balance + full ledger, matching `AccountBalanceCalculator`'s INCOME/EXPENSE/TRANSFER posting rules exactly) and goal-contribution sums against the grilled targets — both done via `mcp__supabase__execute_sql` queries, not on-device (no app code changed, this is pure data). On-device verification is whoever runs the actual screenshot session's job: confirm the pull-sync lands all of the above correctly in Records/Analysis/Budgets/Savings/Combined before capturing.

---

## Item 2 — Google Sign-In (pulled from Post-V1 Horizon #1)

- **Status:** 📋 **PLANNED — booked 2026-07-23. Model: Opus recommended for the build (this booking itself: Sonnet, low effort).** Not yet grilled — needs one before implementation starts. No schema change yet (stays whatever version is current when picked up).
- **Request:** Alvin — book Google Sign-In as a concrete v1.7.0 item (Facebook Login, previously paired with it on the Horizon list, is being shelved instead — see [Item 3](#item-3--facebook-login-shelved-indefinitely)).
- **Origin:** [Post-V1 Horizon #1](project-build-progress.md) — "Google Sign-In — Supabase OAuth + Android Credential Manager." One of the two SSO items explicitly out of V1 scope per `CLAUDE.md` ("Explicitly Out of Scope for V1 — Google / Facebook SSO").
- **Why it needs a grill before build (not a pure pattern-follow):** the codebase has exactly one auth pathway today — Supabase email+password (`AuthScreen.kt`/`AuthViewModel.kt`) — with no existing OAuth plumbing to copy. Open questions a grill should resolve: Android Credential Manager vs. a raw Supabase OAuth redirect flow; how a Google sign-in interacts with **ADR-0013** (`EnsureCurrentUserRowUseCase` must run before any other write — does a first-time Google sign-in synthesize the same users-row bootstrap as email/password registration, and does it route through onboarding the same way); account-linking behavior if the Google email matches an existing email/password account; whether email verification (currently required before first login) is even relevant for a Google-verified identity; and where the sign-in button lives on `AuthScreen.kt` relative to the existing form.
- **Does not change (this booking):** no code, schema, or Room change — this item is a stub only. The [tester-facing roadmap card](../../app/src/main/java/com/iponlove/app/feature/help/presentation/UpcomingFeaturesScreen.kt) already lists "Google Sign-In" (Q3 2026) — untouched, no edit needed.
- **Verify:** N/A — nothing built yet. Next step is `/grilling` before picking this up.

---

## Item 3 — Facebook Login: shelved indefinitely (pulled from Post-V1 Horizon #2)

- **Status:** ✅ **DONE `2ba371c`** — shelved 2026-07-23 (Alvin's call: Google Sign-In alone covers the SSO ask for now; no active interest in a second OAuth provider). Sonnet, low effort — pure doc/roadmap cleanup, mirroring the existing Password-vault/CSV-export shelving precedent exactly (see [deferred.md](deferred.md#shelved--no-active-interest-indefinite)).
- **Request:** defer Facebook Login indefinitely, alongside booking Item 2.
- **Change:**
  - [project-build-progress.md](project-build-progress.md) — Post-V1 Horizon #2 entry updated: moved to `deferred.md`'s Shelved shelf, no longer an active Horizon target.
  - [deferred.md](deferred.md) — new **Facebook Login** entry added under "Shelved — no active interest (indefinite)", same shape as Password vault / CSV export.
  - [UpcomingFeaturesScreen.kt](../../app/src/main/java/com/iponlove/app/feature/help/presentation/UpcomingFeaturesScreen.kt) — the `RoadmapItem` for "Facebook Login" removed from `ROADMAP`, matching the "shelved items aren't tester-facing" convention (Password vault / CSV export were never on this screen either). This is a genuine removal, not the Item 47 "annotate, don't delete" case — that convention is for **shipped** cards; Facebook Login never shipped, so there's nothing to annotate.
  - Google Sign-In's own roadmap card is untouched (still active, target unchanged).
- **Does not change:** any auth code, Supabase config, or the Item 2 Google Sign-In booking.
- **Verify:** `assembleStagingDebug` green (pure list-literal removal, no logic touched); on-device/visual check that Settings → Upcoming features no longer shows a Facebook Login card and every other card is unaffected.

---

## Item 4 — Remove budget-cycle start-day feature (revert ADR-0046 / v1.6.5 Item 10b)

- **Status:** ✅ **DONE `6cbe3ce`** (2026-07-23, Sonnet, medium effort, on-device verified by Alvin). No schema/Room change (the feature itself never touched schema — reverting doesn't either).
- **Request:** Alvin — the payday-aligned budget cycle (1st/10th/15th/25th/end-of-month) only re-windows the Budgets tab; Records/Combined/Analysis/paywall stay calendar-month by design (ADR-0046 §1), so the "matches how you actually experience cash flow" payoff breaks the moment you check Analysis. Not worth the added settings surface + edge-case handling (short-month clamping, personal-vs-shared-calendar split) for a benefit that only lands in one of four places. Decided in conversation, not from beta feedback.
- **Change:** revert to the pre-ADR-0046 calendar-month-only budget cycle. Delete or gut:
  - [BudgetCycle.kt](../../app/src/main/java/com/iponlove/app/feature/budgets/domain/usecase/BudgetCycle.kt) — the cycle-key/window math (or delete outright if nothing else references it after the call sites below revert)
  - [BudgetStartDayRepositoryImpl.kt](../../app/src/main/java/com/iponlove/app/feature/settings/data/BudgetStartDayRepositoryImpl.kt) + the `finance_prefs` DataStore key it owns
  - [ObserveBudgetStartDayUseCase.kt](../../app/src/main/java/com/iponlove/app/feature/settings/domain/usecase/ObserveBudgetStartDayUseCase.kt) (+ its `SetBudgetStartDayUseCase` sibling)
  - [SettingsModule.kt](../../app/src/main/java/com/iponlove/app/feature/settings/di/SettingsModule.kt) — drop the DI bindings for the above
  - [FinanceScreen.kt](../../app/src/main/java/com/iponlove/app/feature/settings/presentation/FinanceScreen.kt) — remove the "Budgets" section (start-day row + `BudgetStartDayDialog` + `BUDGET_START_DAY_PRESETS`); **Currency and Privacy sections stay**, screen survives with two sections
  - [FinanceUiState.kt](../../app/src/main/java/com/iponlove/app/feature/settings/presentation/FinanceUiState.kt) / [FinanceViewModel.kt](../../app/src/main/java/com/iponlove/app/feature/settings/presentation/FinanceViewModel.kt) — drop `budgetStartDay` state + `setBudgetStartDay`
  - Revert cycle-aware call sites to plain calendar `yearMonthKey`: [BudgetProgressCalculator.kt](../../app/src/main/java/com/iponlove/app/feature/budgets/domain/usecase/BudgetProgressCalculator.kt), [BudgetRowsCalculator.kt](../../app/src/main/java/com/iponlove/app/feature/budgets/domain/usecase/BudgetRowsCalculator.kt) (drop the personal-vs-`CALENDAR_START_DAY` shared split — both become calendar), [BudgetsViewModel.kt](../../app/src/main/java/com/iponlove/app/feature/budgets/presentation/BudgetsViewModel.kt) (drop the cycle-range header, back to plain month-name header), [CheckBudgetAlertsUseCase.kt](../../app/src/main/java/com/iponlove/app/feature/budgets/domain/usecase/CheckBudgetAlertsUseCase.kt) and [BudgetAlertWorker.kt](../../app/src/main/java/com/iponlove/app/feature/budgets/worker/BudgetAlertWorker.kt) (drop the pref read, back to plain "current month")
  - [0046-budget-cycle-start-day.md](../adr/0046-budget-cycle-start-day.md) — mark **Superseded/Reversed** with a one-line pointer to this item, rather than deleting the ADR (keeps the rationale + why-it-didn't-stick on record)
  - Tier-1 tests: delete the ADR-0046 cycle-math tests (`BudgetCycle`, non-1-start-day bucketing/rollover/alert cases); the existing calendar-month tests (`startDay = 1` cases) already cover the reverted behavior, no new tests needed
  - **Does not change:** Records/Combined/Analysis/paywall (already untouched by the feature, ADR-0046 §1), the shared couple budget's calendar behavior (already calendar-only, ADR-0047 amendment — just stops being a special case once personal is calendar too), `Budget.yearMonth` schema/format (already an opaque `"2026-07"` label, unaffected either way)
- **Built exactly as planned above** — all five files deleted, all six call sites reverted, `BudgetRowsCalculatorTest`'s one non-1-start-day case removed (its other 3 merge-behavior cases untouched), ADR-0046 marked Superseded/Reversed with a pointer to this item.
- **Verify:** `testStagingDebugUnitTest` + `assembleStagingDebug` green. On-device verified by Alvin: Settings → Finance shows only Currency + Privacy (no Budgets section); budgets render/reset on plain calendar months (no date-range header).

---

## Item 5 — Settings → Finance: currency code labels black-on-black in dark mode

- **Status:** ✅ **DONE `4088449`** — on-device verified by Alvin (Settings → Finance and onboarding's Currency step, both surfaces). **Model: Sonnet, low effort** — a one-line color fix matching an already-diagnosed, already-fixed bug pattern (the Item 8 Slice 6a `MonthStepperRow` / Slice 6h `AccentColorPicker` "black-fallback under transparent Scaffold" bug); no grill needed. No schema/Room change.
- **Request:** Alvin — "settings>finance in dark mode currency names like PHP USD are black font color." Confirmed 2026-07-23 the same bug also shows in the onboarding Currency step.
- **Root cause:** [CurrencySymbolPicker.kt](../../app/src/main/java/com/iponlove/app/core/ui/CurrencySymbolPicker.kt)'s `CurrencySwatch` rendered the currency code caption (`Text(symbol.code, ...)`, e.g. "PHP"/"USD") under the glyph circle with **no explicit color**. Both `FinanceScreen.kt` (Settings → Finance) and `OnboardingCurrencyScreen.kt` host the shared `CurrencyGrid` inside a `Scaffold(containerColor = Color.Transparent)`; `contentColorFor(Color.Transparent)` resolves to `Color.Unspecified`, and Compose's `Text` falls back to its hardcoded black default — same mechanism as the two prior fixes, just never caught on this shared component. Confirmed both surfaces share the exact same `CurrencyGrid` call (no separate onboarding copy), so one fix covers both.
- **Change:** [CurrencySymbolPicker.kt](../../app/src/main/java/com/iponlove/app/core/ui/CurrencySymbolPicker.kt) — gave the code-label `Text` an explicit `color = MaterialTheme.colorScheme.onSurface`, matching the file's existing convention. Did not touch the glyph-circle swatch itself, `CurrencyGrid`'s layout, or any other consumer.
- **Verify:** `assembleStagingDebug` + `testStagingDebugUnitTest` green (pure color-value change, no logic, no new tests needed). On-device verified by Alvin: Settings → Finance and onboarding's Currency step in dark mode both show legible (non-black) "PHP"/"USD"/etc. labels under each currency swatch; light mode unaffected.

---

## Item 6 — Exportable reimbursable-expense reports (incl. receipt photos)

- **Status:** 📋 **PLANNED — booked 2026-07-23. Model: Opus recommended for the build (this booking itself: Sonnet, low effort).** Not yet grilled — needs one before implementation starts. No schema change yet.
- **Request:** Alvin — a user wants to export a report of their reimbursable transactions (the ADR-0049 `exclude_from_analysis` category-flag pair, see [Item 43](v1.6.9.md#item-43)) to submit to their employer, with proper documentation: date, note, amount, and receipt photo proof. Question to resolve: is export feasible if receipt photo attachments need to be bundled in, not just the transaction data?
- **Why it needs a grill before build (not a pure pattern-follow):** several open questions with no existing precedent in the codebase —
  - **Scope conflict:** `CLAUDE.md`'s "Explicitly Out of Scope for V1" list names **CSV / PDF export** directly. This request is exactly that surface, scoped down to one category pair — the grill needs to either get an explicit scope exception from Alvin (and update `CLAUDE.md`) or reframe the ask into something already in-scope (e.g. a share-sheet intent bundling text + images, not a generated document).
  - **No existing export/document-generation pathway.** There's no PDF/CSV writer, no file-sharing (`Intent.ACTION_SEND`/`FileProvider`) plumbing anywhere in the app today — this is greenfield, not a copy-paste pattern.
  - **Receipt photos live in Supabase Storage** (note-image-style attachment, per `CLAUDE.md`'s "Storage: note image attachments only" — receipts reuse that same storage bucket per Item 9/ADR history). Bundling them into an export means either downloading + zipping/embedding into a PDF on-device, or generating a shareable link — a real design decision with offline/large-attachment/cost implications.
  - **Which transactions qualify:** exactly the `exclude_from_analysis`-flagged Reimbursable category, a date range, or ad-hoc selection? Does it scope to one account, one category pair, or any user-chosen filter (this may want to lean on [Item 7](#item-7--records-page-filter-feature)'s filter UI once that lands)?
  - **Premium/paywall interaction:** exports are a natural Premium "delighter" per the monetization section — does this gate behind `is_premium` (ADR-0044) like other delighters, and does the dormant-entitlement build stance (ships OFF until Alvin's go) apply here too?
  - **Output format:** PDF (most "submit to employer" friendly, needs a renderer library) vs. a plain share-sheet bundle (note text + images, cheapest to build, less polished) — a real tradeoff to grill, not a given.
- **Does not change (this booking):** no code, schema, or Room change — stub only.
- **Verify:** N/A — nothing built yet. Next step is `/grilling` before picking this up.

---

## Item 7 — Records page filter feature

- **Status:** 📋 **PLANNED — booked 2026-07-23. Model: Sonnet recommended for the build (this booking itself: Sonnet, low effort).** Not yet grilled — needs one before implementation starts. No schema change yet.
- **Request:** Alvin — add filtering to the Records (Transactions) tab, scoped to the currently-visible month: filter by category, filter by account, and filter by amount-spent range (min/max). **No date filter** — the page already separates transactions by date/day-header, so a date-range filter would be redundant. **Filter only, no sorting.**
- **Why it needs a grill before build (not a pure pattern-follow):** it's a real feature-shape decision, not just wiring an existing pattern, and touches a screen ([TransactionsScreen.kt](../../app/src/main/java/com/iponlove/app/feature/transactions/presentation/TransactionsScreen.kt)/`TransactionsViewModel.kt`) that already carries several combined flows (recurring cards, privacy eye, month stepper) at or near its `combine` ceiling (see Item 8 Slice 6a's note that the ViewModel is already at a 5-flow ceiling in sibling screens) —
  - **UI placement:** a filter icon/bottom sheet (matching the existing `PlayfulDialog`/bottom-sheet vocabulary) vs. an inline `PlayfulChip` row like Categories/Budgets already use for Personal/Shared and category filters — is there a precedent to extend or a new pattern needed?
  - **Filter combination logic:** category + account + amount-range are presumably AND'd together — confirm, and confirm multi-select behavior within a single filter type (e.g. can 2 categories be selected at once, OR'd within category but AND'd against account?).
  - **Persistence:** does the filter reset when the month changes (stepper) or when leaving/re-entering Records? Is it session-only (ViewModel state) or persisted (DataStore), matching or diverging from how privacy mode/palette persist globally?
  - **Empty-state handling:** what renders when a filter combination matches zero transactions in the visible month — a distinct empty state vs. the existing no-transactions empty state?
  - **Archived category/account interaction:** per [v1.6.7 Item 5](v1.6.7.md#item-5)'s label-preservation fix, archived categories/accounts still show on historical rows — should they be filterable too, or excluded from the filter's own picker list (mirroring the picker-vs-display split that fix established)?
  - **Query implementation:** whether this is a new `TransactionDao` query variant (category/account/amount-range params) or client-side filtering over the already-loaded month's list — a real perf/architecture call, not obvious without checking current row-count expectations.
- **Does not change (this booking):** no code, schema, or Room change — stub only. Sorting is explicitly out of scope per Alvin's framing.
- **Verify:** N/A — nothing built yet. Next step is `/grilling` before picking this up.

---

## Item 8 — Prompt to create a counterpart category when saving a reimbursable category

- **Status:** ✅ **DONE `89f57cd`** (2026-07-23, Sonnet, medium effort as booked) — reused the existing `PendingCategoryDelete` confirm-dialog shape already in the same ViewModel (state holds a pending confirmation, `PlayfulDialog` renders it, VM sets/clears it). No schema/Room change — reuses the ADR-0049 `excludeFromAnalysis` flag and the existing `UpsertCategoryUseCase`, no new entity/column.
- **Request:** Alvin — today [Item 43](v1.6.9.md#item-43) (ADR-0049) lets a user flip "Exclude from spending reports" on a category, but nothing nudges them to create the matching counterpart (expense↔income) that makes the pass-through pattern actually work (per the discussion above: without both legs, the balance looks permanently off). When the user taps Save on a category editor with the flag ON, intercept the save and ask whether they want to create the counterpart now, before persisting:
  - Expense reimbursable created → *"Do you want to create an income reimbursement category for this? This is where you'll log when you get paid back."* (Yes/No)
  - Income reimbursable created → *"Do you want to create an expense reimbursement category for this? This is where you'll log when you spend your reimbursement."* (Yes/No) — covers the advance-then-spend direction (e.g. employer gives a phone allowance upfront; the expense leg is logged when the employee actually buys the phone).
- **Confirmed in conversation:**
  - If **Yes** → do **not** auto-name/auto-derive the counterpart. Show a second prompt asking the user to type the counterpart category's name (simple text-input dialog), then create it (opposite `CategoryType`, `excludeFromAnalysis = true`) alongside the original, both via `UpsertCategoryUseCase`. Icon/color default to the same values as the original category (only the name is user-prompted; not worth a second icon/color picker for a same-flow side effect).
  - If **No** (or dialog dismissed) → save just the original category, exactly like today. No forced choice — dismissal behaves like No.
  - This prompt fires on **create** only where the flag is being turned on; **does not** apply retroactively to existing categories that already have the flag (no migration/backfill prompt).
- **Change:**
  - [CategoriesUiState.kt](../../app/src/main/java/com/iponlove/app/feature/categories/presentation/CategoriesUiState.kt) — new pending-confirmation state alongside `PendingCategoryDelete` (e.g. `pendingCounterpartPrompt: Category?` for the Yes/No step, `pendingCounterpartNameInput: Category?` for the name-input step) — two sequential dialogs, not one.
  - [CategoriesViewModel.kt](../../app/src/main/java/com/iponlove/app/feature/categories/presentation/CategoriesViewModel.kt) — `save()` (currently lines 144-168): when `state.excludeFromAnalysis` is true and this is a create (or the flag is newly turned on), branch into the confirm-dialog state instead of calling `UpsertCategoryUseCase` immediately; add handlers for Yes/No and for the name-input submit that create both categories via two `UpsertCategoryUseCase` calls. No new UseCase needed.
  - [CategoriesScreen.kt](../../app/src/main/java/com/iponlove/app/feature/categories/presentation/CategoriesScreen.kt) — two new `PlayfulDialog` renders (Yes/No prompt, then name-input prompt), copy-worded per direction as above, mirroring how the delete-confirm dialog is rendered near lines 207-233.
  - **Does not change:** `Category` domain model, `UpsertCategoryUseCase`, `CategoryRepository`/`CategoryRepositoryImpl` (the `excludeFromAnalysis` passthrough there is already correct, per earlier on-device fix), the onboarding-seeded "Reimbursable"/"Reimbursement" pair, or any Analysis/Budgets/Combined exclusion logic.
- **Built as planned, with one deliberate deviation:** the original category is saved **immediately** on Save (unchanged from today), not deferred until the Yes/No answer as the "Change" bullet above sketched — if the app is killed between Save and answering the prompt, the category the user actually asked for survives; only the optional counterpart is lost. Verified empirically (see below).
- **Follow-up refinement (mid-build, Alvin's request):** step 2's bare "Name" field had no context, unlike step 1's full sentence. Added the same direction-specific helper sentence above the field (extracted into a shared `counterpartHelperText()` so both dialogs can't drift out of sync) plus an "e.g. Reimbursement" placeholder in the field itself.
- **Verify:** `testStagingDebugUnitTest` green — 161/161 including 7 new `CategoriesViewModelTest` cases (expense→income, income→expense, flag-off no-op, edit-never-fires, blank-name guard, full create flow with icon/color inheritance, dismiss-is-No). `assembleStagingDebug` green. On-device: expense→income and income→expense directions confirmed by Alvin; explicit No, scrim-tap dismiss, edit-does-not-fire, blank-name guard, and abandon-mid-flow (force-stop between "Yes" and naming, to test the deviation above) all verified on an emulator, cross-checked against the local Room DB (`excludeFromAnalysis`/`type`/icon/color columns all correct on every row, no orphan rows from the abandoned attempt).

---

## Item 9 — Couple banner: derived vs. own toggle (Item 3 Leg 2, pulled from Post-V1 Horizon #9)

- **Status:** 📋 **PLANNED — booked 2026-07-23. Model (once grilled): TBD, likely Opus** — touches shared couple-state rendering (ADR-0004/0005/0011's partner-data boundary), not a pure pattern-follow. **Not yet grilled** — needs `/grilling` before implementation starts. No schema/Room change yet.
- **Request:** originally [Post-V1 Horizon #9](project-build-progress.md) ("Profile & couple photo upload"), pulled into [v1.6.7.md Item 3](v1.6.7.md#item-3--profile--couple-photo-upload-pulled-from-post-v1-horizon-9) and grilled 2026-07-22 (Opus session), which staged the resolved concept into 3 legs of differing weight. **Leg 1 (motif avatars) shipped** as v1.6.7 Item 3 (`f75cf4d`). Legs 2 + 3 were said to be "deferred to v1.6.8.md," but v1.6.8's charter was reset to a single-fix release (2026-07-22) before either leg got its own item number or grill — they fell through the cracks (never carried into [deferred.md](deferred.md), and the stale Horizon #9 line still doesn't reflect Leg 1 shipping). Re-booked here so they're tracked again.
- **The open design question (from the 2026-07-22 grill outcome, not yet resolved):** a "couple banner" — does it *derive* automatically (e.g. composited from both partners' motif avatars / accent colors, no user action) or is it something a couple sets *as its own* asset (an explicit couple-level upload/pick, separate from either partner's personal avatar)? This derived-vs-own framing is the entire grill; nothing past it is decided.
- **Does not change (this booking):** no code, schema, or Room change — stub only.
- **Verify:** N/A — nothing built yet. Next step is `/grilling` before picking this up.

---

## Item 10 — Premium couple-photo upload (Item 3 Leg 3, pulled from Post-V1 Horizon #9)

- **Status:** 📋 **PLANNED — booked 2026-07-23. Model (once grilled): TBD, likely Opus** — spans Storage/RLS, the entitlement/`PlanLimits` gate (ADR-0044), and couple-shared state; not a pure pattern-follow. **Not yet grilled** — needs `/grilling` before implementation starts. No schema/Room change yet. **Soft-blocked regardless of grill outcome:** entitlement enforcement is dormant app-wide (kill-switch OFF per CLAUDE.md's Monetization section) until the post-beta flip, so a premium-gated feature can be built but won't visibly lock/unlock until that flip happens.
- **Request:** same origin as [Item 9](#item-9--couple-banner-derived-vs-own-toggle-item-3-leg-2-pulled-from-post-v1-horizon-9) above — Leg 3 of the [v1.6.7.md Item 3](v1.6.7.md#item-3--profile--couple-photo-upload-pulled-from-post-v1-horizon-9) grill (2026-07-22), also lost when v1.6.8.md's charter reset before it got its own item number. Re-booked here.
- **The open design surface (from the 2026-07-22 grill outcome, not yet resolved):** a real Supabase Storage bucket + RLS policy for couple/profile photo upload, image crop + size cap, and a personal-vs-joint distinction (is this a per-user photo, a per-couple photo, or both?) — plus where it plugs into the `Feature`/`PlanLimits` premium gate (per [subscription-paywall-design.md](subscription-paywall-design.md)). None of this is decided past the one-line sketch in the source grill.
- **Does not change (this booking):** no code, schema, Storage bucket, or Room change — stub only.
- **Verify:** N/A — nothing built yet. Next step is `/grilling` before picking this up.

---

## Item 11 — Appearance: "Follow system" dark/light mode option

- **Status:** 📋 **PLANNED — booked 2026-07-23. Model: Sonnet, low–medium effort** — replaces a `Boolean isDark` with a 3-state `ThemeMode` enum threaded through an existing DataStore-backed settings flow; no grill needed, design fully specified below. No schema/Room change (DataStore-only, not Room) — the `theme_is_dark` boolean key is replaced by a new string key (dev phase, no live users to migrate per [dev-phase-no-prod]).
- **Request:** Alvin — asked whether appearance (dark/light) can follow the OS system setting, and how hard it'd be. Confirmed: today it's a plain user-chosen Light/Dark switch with no system-follow option (`isSystemInDarkTheme()` isn't used anywhere in the codebase); adding a 3rd "System" state is a small, mechanical change.
- **Change:**
  - New `ThemeMode` enum (`LIGHT`, `DARK`, `SYSTEM`) in `feature/settings/domain/model/`, replacing `isDark: Boolean` on [ThemePreferences.kt](../../app/src/main/java/com/iponlove/app/feature/settings/domain/model/ThemePreferences.kt) with `mode: ThemeMode = ThemeMode.SYSTEM` (system as the new default, since it's the least surprising to a first-run user — currently defaults to light).
  - [ThemeRepositoryImpl.kt](../../app/src/main/java/com/iponlove/app/feature/settings/data/ThemeRepositoryImpl.kt) — swap the `booleanPreferencesKey("theme_is_dark")` for a `stringPreferencesKey("theme_mode")` storing the enum name; no migration path needed (dev phase, no live users).
  - [Theme.kt](../../app/src/main/java/com/iponlove/app/core/ui/theme/Theme.kt) — `IponTheme` (line 32) resolves the effective dark flag as `mode == SYSTEM && isSystemInDarkTheme() || mode == DARK` before building the color scheme; this is the one call site that newly uses `isSystemInDarkTheme()`.
  - `ThemeDraftRepository` (ADR-0014 live-preview state) — same 3-state resolution for the Appearance screen's live preview.
  - [AppearanceScreen.kt](../../app/src/main/java/com/iponlove/app/feature/settings/presentation/AppearanceScreen.kt) — replace the binary Light/Dark `Switch` (lines 126-140) with a 3-way Light/Dark/System control (segmented buttons or 3 `PlayfulChip`s, matching the palette grid's selection pattern); `AppearanceViewModel`'s `toggleDarkMode()` becomes `setThemeMode(mode)`.
  - `MainActivity.kt` (line ~194, `setContent`) and `QuickAddActivity.kt` — both already just pass `themePreferences` through to `IponTheme`; no change needed beyond the type swap propagating.
  - **Does not change:** `ThemePalette` (color palette selection, unrelated axis), `accent_color` on the synced `users` row (ADR-0014 — couple attribution color, separate from personal theme), any Room table/schema.
- **Verify:** `testStagingDebugUnitTest` + `assembleStagingDebug` green (add/update a `ThemeRepositoryImplTest`-style case if one exists for the old boolean key, covering the 3 enum values round-tripping through DataStore). On-device: toggle device system dark mode with the app set to "System" and confirm the app follows without restart (Compose recomposes `isSystemInDarkTheme()` automatically); explicit Light/Dark selections stay pinned regardless of system setting; live preview in Appearance still updates immediately on tap (ADR-0014).

---
