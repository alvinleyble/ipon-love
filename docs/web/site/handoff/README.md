# Handoff — Marketing Site Seed Context

Generated from the `ipon-love` repo on 2026-07-31, for bootstrapping a fresh Claude Code session in the new `ipon-love-site` repo. Point that session's first read at this folder so it isn't starting from zero — while staying decoupled from `ipon-love`'s own `CLAUDE.md`, which is Android/Kotlin/ADR-specific and doesn't apply to a static site.

**What's in here:**
- [`CLAUDE.md`](CLAUDE.md) — a draft seed for the new repo's own `CLAUDE.md`. Copy it in and trim as the project takes shape — starting point, not gospel.
- [`brand-context.md`](brand-context.md) — the app's pitch, feature list, terminology, and monetization blurb, for writing site copy without needing the full app repo.
- [`links-and-refs.md`](links-and-refs.md) — the current Play Console link (testing-only, not the public listing) and where screenshot assets will eventually come from.

**Deliberately left out:** anything Android/Kotlin/ADR/sync-specific (none of it applies here), the stale 2026-07-06 subscription-monetization memory (superseded — the one-time-₱249 pivot on 2026-07-07 is current and is what's reflected in `brand-context.md`), and staging DB test credentials (no reason a marketing site needs those).

Source doc this was distilled from: [`../site-build-progress.md`](../site-build-progress.md), one level up in this same repo.
